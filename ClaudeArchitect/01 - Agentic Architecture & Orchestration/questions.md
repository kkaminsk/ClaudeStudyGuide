# Domain 1: Agentic Architecture & Orchestration — Practice Questions

**Instructions:** Choose the single best answer for each question.

---

**1.** When Claude returns a `stop_reason` of `"tool_use"`, what must your application include in the follow-up `tool_result` message?

A) The tool's `name` and `description`
B) The `tool_use_id` from the original `tool_use` block
C) A new unique `request_id` generated by your application
D) The full conversation history up to that point

---

**2.** What is the primary difference between building an agentic loop on the Messages API versus using the Agent SDK?

A) The Messages API supports tool use; the Agent SDK does not
B) The Agent SDK manages the loop, sessions, and permissions; the Messages API is stateless and requires you to build those yourself
C) The Messages API automatically persists sessions to disk
D) The Agent SDK only supports single-turn interactions

---

**3.** In the Agent SDK, what happens when both `max_turns` and `max_budget_usd` are set and the turn limit is reached first?

A) The agent continues until the budget is also exhausted
B) The agent returns a `ResultMessage` with an `error_max_turns` subtype
C) The agent silently drops the remaining budget and exits
D) The SDK raises an unrecoverable exception

---

**4.** What is a documented anti-pattern that can cause Claude to return empty `end_turn` responses during a tool-use loop?

A) Sending tool results with `is_error: true`
B) Using `disable_parallel_tool_use=true`
C) Adding extra text blocks immediately after `tool_result` blocks in the same user message
D) Setting `max_tokens` too high

---

**5.** How do Agent SDK subagents manage context relative to their parent agent?

A) They share the parent's full conversation history
B) They receive a fresh context with only the Agent tool's prompt string; only the subagent's final message returns to the parent
C) They inherit the parent's history but cannot make tool calls
D) They run in the same context window as the parent

---

**6.** Which multi-agent organizational pattern does Anthropic's orchestrator-workers cookbook directly demonstrate?

A) Peer-to-peer gossip protocol
B) Blackboard shared-workspace architecture
C) Master-worker pattern where a central agent decomposes tasks and delegates to specialized workers
D) Broker/message-bus publish-subscribe

---

**7.** In the Agent SDK, what is the difference between "continue," "resume," and "fork" session operations?

A) Continue finds the most recent session; resume targets a specific session by ID; fork copies history and diverges
B) Continue replays only tool results; resume replays all messages; fork deletes the original session
C) All three are aliases for the same operation
D) Continue starts fresh; resume appends; fork merges two sessions

---

**8.** Why should tool execution in an agentic loop be designed for idempotency?

A) Claude always sends each tool call exactly twice
B) Tool calls can have side effects, and retries or replays should not re-execute operations that have already completed
C) The Messages API automatically retries all tool calls
D) Idempotent tools run faster than non-idempotent ones

---

**9.** What is the safest default when configuring a subagent's available tools?

A) Give it the same tools as the parent including the `Agent` tool
B) Give it all tools plus `bypassPermissions`
C) Omit the `Agent` tool from the subagent's tool list to prevent unintended recursion
D) Give it no tools at all

---

**10.** How does prompt caching work with the Agent SDK's tool-use loop across multiple turns?

A) Each turn is cached independently with no prefix reuse
B) Stable prefixes like system prompts, tool definitions, and CLAUDE.md content are cached, reducing cost and latency for repeated prefixes across turns
C) Prompt caching is disabled during tool-use loops
D) Only the final response is cached
